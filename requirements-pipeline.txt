# Pipeline dependencies (the grader itself needs only requirements.txt).
# Pinned to what the reported scores were produced with.
numpy==2.4.6
pillow==12.3.0
opencv-python-headless==5.0.0.93
torch==2.13.0
torchvision==0.28.0
ultralytics==8.4.137
